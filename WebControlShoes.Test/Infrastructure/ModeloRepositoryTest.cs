using Microsoft.EntityFrameworkCore;
using System;
using System.Collections.Generic;
using System.Threading.Tasks;
using Xunit;
using Zapatillas.Domain.Entities;

namespace UnitTest.Infrastructure
{
    public class ModeloRepositoryTest
    { 
        /*
        [Fact]
        public async Task GetAllModelos()
        {
            // 
            DbContextOptionsBuilder options = new DbContextOptionsBuilder().UseInMemoryDb();
            var db = new AppDbContext(options.Options);
            db.Authors.Add(new Modelo("asdasd","cara")); 
            await db.SaveChangesAsync();

           // var repo = new ModeloRepository(db);

            // 
            IEnumerable<Modelo> modelos = await repo.GetAllAsync();

            // Assert
            Assert.Single(modelos);
        }*/
    }
}
